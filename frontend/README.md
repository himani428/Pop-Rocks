# Pop Rocks Dance Academy — Frontend

React + Vite frontend. See the root `README.md` (one folder up) for full setup and deployment
instructions.

Quick start:

```bash
npm i
npm start
```

Runs at http://localhost:5173 and expects the backend at the URL in `.env` (`VITE_API_URL`).
